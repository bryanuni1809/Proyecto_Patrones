package com.mycompany.Combate.Atk;
import com.mycompany.Model.pokemon.Pokemon;
import java.util.List;

public final class CombateMemento {
    /*
    private final List<Integer> hpList;
    private final Pokemon[] equipo;
    private final Estado estado;

    public CombateMemento(List<Integer> hpList, List<Pokemon> equipo, Estado estado) {
        this.hpList = List.copyOf(hpList);
        this.energyList = List.copyOf(energyList);
        this.estado = estado;
    }

    public List<Integer> getHpList() { return hpList; }
    public List<Integer> getEnergyList() { return energyList; }
    //public int getEstado() { return estado; }

    public static CombateMemento fromPokemons(List<Pokemon> pokemons, int turno) {
        List<Integer> hps = pokemons.stream().map(Pokemon::getHp).toList();
        List<Integer> energies = pokemons.stream().map(Pokemon::getEnergy).toList();
        return new CombateMemento(hps, energies, turno);
    }
*/
}

